#ES6 AngularJS Boilerplate This project is an example of using ECMA 6 with AngularJS.

Getting Started
---------------

To run this application in development mode:

```
$ gulp
```

To run this application is dist mode:

```
$ npm start
```
